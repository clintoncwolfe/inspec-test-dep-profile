name: inspec-test-dep-profile
license: Apache-2.0
summary: An inspec profile that depends on a 3rd party profile by means of a git dependency
version: 0.1.0
supports:
  platform: os
depends:
  - name: ssh-baseline
    git: git@github.com:dev-sec/ssh-baseline.git
